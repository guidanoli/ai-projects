solverapp
=========

Main application, responsible for:
* Running heuristics over instance files
* Monitoring progress on time
* Exporting results in CSV format

Command line arguments
----------------------

For information about how to use the application
in the command line, be sure to run it on your
favourite terminal with the parameter --help:

$ solverapp --help